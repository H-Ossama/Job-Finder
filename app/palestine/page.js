import BlockedRegionPage from '@/components/BlockedRegionPage';

export const metadata = {
    title: 'Free Palestine 🇵🇸',
    description: 'We stand in solidarity with the Palestinian people. From the river to the sea, Palestine will be free.',
};

export default function PalestinePage() {
    return <BlockedRegionPage />;
}
